updated_at: 2026-05-27
repository: Luce-Org/lucebox-hub
integration_branch: auto-integration
remotes:
  upstream: origin
  writable: easel
heads:
  upstream_main: 2540f19
  integration_head: 14ae91e
stack:
  included:
    - pr: 274
      status: already-on-branch
      note: pflash early-exit drafter stack already present on easel/auto-integration
    - pr: 275
      status: already-on-branch
      note: draft default follow-up already present on easel/auto-integration
    - pr: 276
      status: already-on-branch
      note: Qwen3.6 Claude-code tool-calling fix already present on easel/auto-integration
    - pr: 264
      status: already-on-branch
      note: adaptive keep_ratio bandit MVP already present on easel/auto-integration
    - pr: 142
      status: already-on-branch
      note: Gemma4 FP16 safetensors drafter support already present on easel/auto-integration
    - pr: 152
      status: already-on-branch
      note: Gemma4 backend helpers already present on easel/auto-integration
  attempted_this_run:
    - pr: 48
      status: stale_or_duplicate
      evidence: dflash/CMakeLists-path change is already represented in current server/CMakeLists; merge into this branch only hits path-level delete/modify churn.
    - pr: 237
      status: blocked_by_rename_conflicts
      evidence: merging pr-237 into auto-integration hits conflicts in server/CMakeLists, backend_factory.{cpp,h}, step_graph.h, qwen35 backend/loader/graph files, and test_dflash; the local pr-237 branch is a much broader dflash-path stack that would require a larger path-migration pass than this run can safely finish.
  held_or_unsalvageable:
    - pr: 237
      reason: blocked_by_rename_conflicts
      evidence: same as attempted entry; high-risk, multi-file path migration.
  upstream_sync:
    - merged_origin_main: true
    - note: upstream main currently includes merged PRs #282 and #283.
  tests_and_checks:
    - "git merge --no-edit origin/main"
    - "git merge --no-commit --no-ff origin/pr/48 (aborted after modify/delete conflict)"
    - "git merge --no-commit --no-ff origin/pr/237 (aborted after multi-file rename conflicts)"
  notes:
    - The integration branch should continue to track upstream main first; this run refreshed it before any PR trials.
    - No new PR was safely added this run; current work was limited to upstream refresh and integration-state recording.
    - Next candidates to triage are the smaller green-check PRs that touch the same renamed area only after a path strategy is agreed.
