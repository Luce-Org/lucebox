# Orion build fixture

This tiny repository exists only for deterministic coding-agent latency tests.
Its names and constants are intentionally synthetic so a model must inspect the
tools' output rather than rely on memorized public code.
